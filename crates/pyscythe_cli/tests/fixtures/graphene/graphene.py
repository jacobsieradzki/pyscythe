"""Enough of graphene for the fixture to resolve its bases without it installed."""


class ObjectType:
    """A schema type; the executor resolves its fields."""


class Mutation:
    """A mutation; the executor calls `mutate`."""
